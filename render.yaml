services:
  - type: web
    name: scary-site
    env: node
    buildCommand: npm install
    startCommand: node -r dotenv/config work.cs
    envVars:
      - key: EMAIL_USER
        sync: false
      - key: EMAIL_PASS
        sync: false
      - key: NODE_ENV
        value: production
